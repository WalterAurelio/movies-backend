import request from 'supertest';



describe('POST /register', () => {
  describe('given a valid firstname, lastname, email and password', () => {
    // const requestPromise = request()

    test('responds with a 200 status code', async () => {})
  });
});